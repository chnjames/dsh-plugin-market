# dsh-plugin-market v0.3.0 — DSH 内嵌 UI + Vercel 网站完整方案

> 调研日期：2026-08-14
> 基于对 DSH Web UI 实际 Slot 树、Service 契约、Builtin 签名、主题 token 的运行时勘察

---

## 一、结论摘要

你的两个想法都**可行且架构优雅**，DSH 提供了专门的加性扩展点：

| 形态 | 入口 Slot | 数据链路 | 状态 |
|---|---|---|---|
| **A. DSH 内嵌弹窗** | `sidebar.footer.action`（按钮）+ `shell.overlay`（弹窗） | `pluginMarket` 提升为 Remote 服务，client 经 `ctx.remote.pluginMarket.*` 调用 | ✅ 完全可行，复用现有 host 服务 |
| **B. Vercel 网站** | 独立 Next.js 静态站 | 构建时抓取 GitHub/npm 生成静态 JSON | ✅ 可行，复用 classifier/indexing 逻辑 |

---

## 二、方案 A：DSH 内嵌「插件市场」按钮 + 弹窗

### 2.1 运行时勘察结论（已实测确认）

**① 左下角按钮位：`sidebar.footer.action`**
- `kind: list`，`scope: root`，`replaceRisk: none`（纯加性，安全）
- 注册：`{ id, order?, label?, locale?, inject? }`
- Props：`{ wide: boolean }`（侧栏宽/窄轨状态）
- 现有 occupant：`cordis-panel`（Cordis 面板按钮，由 `dsh-client-ui-cordis` 注册）——**同类先例**
- 位置：设置按钮旁（`sidebar.settings` 之上/之旁）

**② 弹窗位：`shell.overlay`**
- `kind: list`，`scope: root`，`replaceRisk: none`，当前无 occupant
- 全屏浮层，**默认 click-through**（条目自行 opt-in pointer-events）→ 弹窗遮罩由组件控制
- 渲染：`renderSlot("shell.overlay", {})`（AppFrame 内，位于所有列之上）

**③ client↔host 数据链路**
- client 插件注入 `remote` 服务，用 `ctx.remote.<ServiceName>.<method>()` 调用 host 的 Remote 服务
- 现例：`ctx.remote.pluginInventory.list()`（插件清单 Tab 就是这么做的）
- **当前缺口**：`pluginMarket` 是用 `ctx.provide` 注册的普通服务，**不是 Remote** → 需提升

**④ client 插件技术栈**
- Builtin：`React`（无 JSX，用 `React.createElement`）、`styles.insert(css)`（package 样式）、`host.call`（动态插件 RPC）、`ctx.slots.register/inject`、`ctx.locale`
- 主题：直接用 `--dsw-alias-*` CSS 变量（`bg-layer-1/2/overlay`、`border-l1/l2`、`label-primary/secondary`、`brand-primary`、`state-*`），明暗自动跟随

### 2.2 实现结构

```
dsh-plugin-market/
├── src/
│   ├── index.ts               # host：现有 apply + pluginMarket 服务
│   ├── remote.ts              # 新增：pluginMarket Remote 服务声明（供 client 调用）
│   ├── ...（indexing/installer/cache 不变）
│   └── client/
│       ├── index.ts           # client 插件入口：注册两个 Slot
│       ├── FooterButton.tsx   # sidebar.footer.action 按钮（wide 轨道适配）
│       ├── MarketModal.tsx    # shell.overlay 弹窗（搜索/分类/卡片/详情/安装）
│       └── market.module.css  # 样式（--dsw-alias-* 变量）
├── package.json               # 增加 "dsh": { "client": { "platform": "web", "inject": [...] } }
└── client.js                  # client 半区 bundle 入口
```

### 2.3 关键实现决策

1. **Remote 服务提升**：把 `pluginMarket` 从 `ctx.provide` 改为 Remote 声明（参照 `pluginInventory` 的 Gateway 模式：`class PluginMarketGateway { @Remote('list') ... }` + `inject: ['loader'?]`），暴露 `search/detail/categories/install/uninstall/list/status`。client 注入 `remote.pluginMarket` 即可调用。
2. **按钮**：`sidebar.footer.action` 注册 `id: 'plugin-market'`，图标 + 窄轨时仅图标、宽轨显示「插件市场」文本。
3. **弹窗**：`shell.overlay` 注册 `id: 'plugin-market-modal'`，内部管理 open/close 状态（按钮点击置 open），遮罩层 opt-in pointer-events，Escape 关闭，点击遮罩关闭。
4. **UI**：设置内 React Tab；公开浏览面是 Vercel `website/`。
5. **独立端口 3789 已删除**：本机不再启 HTTP 面板；浏览走 Vercel，安装走设置 Tab。

### 2.4 工作拆分（A1→A5）

| 步骤 | 内容 | 工作量 |
|---|---|---|
| A1 | host：`pluginMarket` 提升为 Remote 服务 | 小 |
| A2 | client 骨架：注册 footer 按钮 + overlay 弹窗 | 小 |
| A3 | 弹窗 UI：React 组件（复用现有逻辑） | 中 |
| A4 | 主题适配 + `dsh.client` manifest + 构建 client bundle | 中 |
| A5 | 发布 v0.3.0 + 端到端验证 | 中 |

---

## 三、方案 B：Vercel 网站（静态生成 B1）

### 3.1 定位
公开落地页 + 在线插件目录，与内嵌弹窗互补（内嵌服务已装用户，Vercel 面向潜在用户引流、SEO）。

### 3.2 技术选型
- **Next.js**（App Router）+ **Tailwind**，部署 Vercel
- **静态生成**：构建时抓取 GitHub API（topic: dsh-plugin）+ npm registry（keyword: dsh-plugin），生成 `public/plugins.json`（复用 host 侧 `classifier.ts` 的分类/风险推断逻辑，抽成共享函数）
- **Vercel Cron**（`vercel.json` 定时）触发重建保持数据新鲜；或手动 `Sync` webhook

### 3.3 页面
- 首页：统计（插件数/分类数）+ 搜索 + 热门榜单
- 分类页：按 12 分类浏览
- 详情页：README 渲染、作者、版本、许可证、风险、GitHub/npm 跳转（`[id].tsx` 动态路由）
- 安装引导：每插件显示 `dsh plugin --profile web add <spec>` 命令 + 复制按钮

### 3.4 与方案 A 的关系
- 共享：`classifier.ts`（分类/风险）、插件数据模型（types.ts）
- 独立：Vercel 无 DSH 运行时，纯静态展示 + 复制命令引导，不做真实安装

---

## 四、推荐实施顺序

1. **A1**：host 侧 Remote 服务（半小时）
2. **A2+A3**：client 插件（动态验证 → 静态固化）
3. **A4**：主题/bundle/manifest
4. **A5**：v0.3.0 发布验证
5. **B**：Vercel 静态站（抽共享逻辑 → Next.js → 部署）

> 动态插件（`cordis_define`）用于 A2/A3 的**快速验证**（DSH 运行时直接加载 JS，无需构建管线），验证通过后固化为 `src/client/` + client bundle 进入 v0.3.0 静态发布。
